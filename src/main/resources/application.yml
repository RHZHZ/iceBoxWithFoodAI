spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myfood
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
# 开启自动驼峰命名映射（column下划线 → property驼峰）
mybatis:
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.rhzhz.mapper: DEBUG # 替换为你的 Mapper 包路径
    org.springframework.web.reactive: DEBUG
    reactor.netty.http.client: DEBUG
ai:
  providers:
    openai:xxxxxx
      api-key: sk-ofHJpRbx'x'x'x'x'x
      base-url: https://x'x'x/v1
    deepseek:
      api-key: sk-e6ad7995x'x'x'x'xc27xxxxx
      base-url: https://api.deepseek.com/v1
      model: deepseek-chat  # 指定使用 R1 模型
